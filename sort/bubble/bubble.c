#include <stdlib.h>
#include "mylist.h"


void	swap_node()
void	bubble(t_list *head)
{
	t_list	*i;
	t_list	*j;

	i = head;
	while (h != NULL)
	{
		j = i;
		while (j != NULL)
		{
			if (i->data > j->data)
			{
				
			}
		}
	}
}
